<main class="container">
  <%= link_to sanitize("&larr; Back to restaurant"), restaurant_path(@restaurant) %>
  <div class="header">
    <h1>New date</h1>
  </div>
  <%= turbo_frame_tag @line_item_date do %>
    <%= render "form", restaurant: @restaurant, line_item_date: @line_item_date %>
  <% end %>
</main>
